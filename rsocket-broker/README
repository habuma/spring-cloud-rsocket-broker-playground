This is a simple spring-cloud-rsocket broker. 

Run with:

$ ./mvnw spring-boot:run

Or build it and run with:

$ ./mvnw package
$ java -jar target/rsocket-broker-0.0.2-SNAPSHOT.jar

It starts up on port 7003.
